<!DOCTYPE html>
<html>
  <head>
    <title>MinnanoRamen</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag 'application' %>


    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/e3532b565d.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="custom-header shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <%= link_to root_path, class: "navbar-brand" do %>
            <%= image_tag "LYOR7363.PNG" %>
          <% end %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto extra-bold" style="font-size: 20px;">
              <li class="nav-item mt-auto"><%= link_to "About", homes_about_path, class: "nav-link" %></li>
              <% if user_signed_in? %>
                <li class="nav-item mt-auto"><%= link_to "投稿", new_post_path, class: "nav-link" %></li>
                <li class="nav-item mt-auto"><%= link_to "ランキング", rank_path, class: "nav-link" %></li>
                <li class="nav-item mt-auto"><%= render 'layouts/notification' %></li>

                <!-- プロフィール画像のドロップダウンメニュー -->
                <li class="nav-item mt-auto dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <%= render 'users/profile_image' %>
                  </a>
                  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                    <a class="dropdown-item" href="<%= mypage_path %>">マイページ</a>
                    <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="delete">ログアウト</a>
                  </div>
                </li>

                <!-- 検索バー -->
                <li class="nav-item mt-auto">
                  <%= render 'searches/search' %>
                </li>
              <% else %>
                <li class="nav-item mt-auto"><%= link_to "会員登録", new_user_registration_path, class: "nav-link" %></li>
                <li class="nav-item mt-auto"><%= link_to "ログイン", new_user_session_path, class: "nav-link" %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <%= flash[:notice] %>
      <%= yield %>
    </main>
    <footer style="position: fixed; bottom: 0; width: 100%;">
      <footer style="position: fixed; bottom: 0; width: 100%;">
      <div class="footer bg-light" style="height: 25px;">
      	<div class="container">
      		<div class="row justify-content-center">
            © 2023 Ryota Kobayashi
            <% if current_user.try(:admin?) %>
              <%= link_to '管理者ログイン', rails_admin_path %>
            <% end %>
          </div>
      	</div>
      </div>
    </footer>
  </body>
</html>
