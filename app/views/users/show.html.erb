<div class="container mt-4">
  <div class="row mx-auto">
    <div class="col-4 mx-auto text-center">
      <% if current_user == @user %>
        <h2>マイページ</h2>
      <% else %>
        <h2>ユーザー詳細</h2>
      <% end %>

      <p><%= image_tag @user.get_profile_image(100,100), class: "rounded-circle" %></p>
      <div>
        <span>
          <span><%= @user.name %></span>
          <span>
            <%= render 'relationships/btn', user: @user %>
          </span>

          <% unless @user.guest_user? || current_user != @user %></span>  <!-- ゲストユーザー以外の時に編集ボタンを表示させる -->
            <%= link_to edit_user_path(@user) do %>
              <span><i class="fas fa-cog" style="color: #cdcad3;"></i></span>
            <% end %>
          <% end %>
        </span>
      </div>

      <div>
        <span>
          <%= link_to user_followings_path(@user) do %>
            フォロー<%= @user.followings.count %>人
          <% end %>
          <%= link_to user_followers_path(@user) do %>
            フォロワー<%= @user.followers.count %>人
          <% end %>
        </span>
      </div>
      <div>
        <span>
          total🍜<%= @user.posts.count %>件
          total👍<%= @user.total_favorites %>件
          total💬<%= @user.total_post_comments %>件
        </span>
      </div>
    </div>
  </div>
  <div class="row">

      <% @user.posts.each do |post| %>
        <div class="col-md-4">
          <div class="card border-secondary d-flex align-items-center justify-content-center" style="height: 28rem; border: 15px solid red;">
            <%= link_to post_path(post) do %>
              <%= image_tag post.convert_to_webp, class: "card-img-top " %>
            <% end %>
          </div>
        </div>
      <% end %>

  </div>
</div>